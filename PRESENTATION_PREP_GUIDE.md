# 🎓 คู่มือเตรียมสอบพรีเซนต์ (Survival Guide)
## หัวข้อ: University Photo Club Gear Rental System

งานนี้เป็นงานคู่และอาจารย์คาดหวังความเป็น **Fullstack** (รู้เขารู้เรา)
คอนเซปต์คือ: **"งานเราต้องเป๊ะ งานเพื่อนต้องรู้เรื่อง (พอแถได้)"**

---

### 🔥 Top 3 หัวข้อที่โดนถามแน่ๆ (ห้ามตายตรงนี้)

ถ้าตอบพวกนี้ไม่ได้ คะแนนหายวูบแน่นอน เตรียมคำตอบให้เนียนครับ

#### 1. "อัลกอริทึมเช็คคิวชน (Overlap Logic)"
> **ความสำคัญ:** นี่คือสมองของระบบ ถ้าไม่มีอันนี้ ระบบก็มั่ว จองซ้อนกันเละเทะ
*   **Concept (ต้องแม่น):** ท่องสูตรนี้ไว้ `StartA < EndB AND EndA > StartB` (ช่วงเวลา A เริ่มก่อน B จบ และ จบหลัง B เริ่ม = ชนกัน)
*   **Frontend พูดว่า:** "ก่อนกดจอง Frontend จะยิง API `/rentals/check-overlap` ไปเช็คเงียบๆ ก่อนครับ ถ้าชน Frontend จะโชว์ Warning และให้ User เลือกว่าจะเปลี่ยนวันหรือจองทับก็ได้ (Soft Reservation)"
*   **Backend พูดว่า:** "ผมมี `RentalValidationService.checkOverlap()` ที่ใช้ QueryBuilder ดักไว้อีกชั้นครับ โดย Query จะกรองเฉพาะรายการที่สถานะยังไม่จบ (ไม่ใช่ RETURNED, REJECTED, CANCELLED) เพื่อเช็คว่าชนกันหรือเปล่า"

#### 2. "ระบบยืนยันตัวตน (Authentication Flow)"
> **ความสำคัญ:** ความปลอดภัยพื้นฐาน ถ้าอธิบาย Flow ไม่ถูกคือจบ
*   **Flow ที่ต้องเล่า:** User กรอก ID/Pass → Backend เช็คด้วย `bcrypt.compare()` → สร้าง JWT Token ด้วย `jwtService.sign()` → ส่งกลับให้ Frontend → Frontend เก็บลง `localStorage.setItem('token', ...)` → และแนบ Token นี้ไปกับ **ทุก Request** ผ่านตัว Interceptor
*   **คำถามวัดกึ๋น:** "ทำไมใช้ JWT ไม่ใช้ Session?"
    *   *คำตอบ:* "เพราะเราออกแบบเป็น Stateless ครับ อนาคตถ้าคนใช้เยอะ ก็ขยาย Server ง่าย ไม่ต้องมานั่ง Sync Session กัน และถ้าจะทำ Mobile App ต่อ JWT ก็คุยง่ายกว่าครับ"

#### 3. "การจัดการ State (State Management)"
> **ความสำคัญ:** วัดว่าเราเข้าใจ React จริงไหม หรือแค่ก๊อปวาง
*   **Topic:** `CartContext`
*   **Frontend พูดว่า:** "ผมใช้ Context API สร้าง GlobalState สำหรับ **รายการจอง** ครับ ที่เจ๋งคือมันจะหมดอายุอัตโนมัติใน **15 นาที** เพื่อไม่ให้คนกั๊กของ โดยผมเซ็ต `setInterval` ทุก 1 วินาทีให้มาเช็คว่ารายการไหนหมดเวลาแล้วก็ลบทิ้งเองเลยครับ"
*   **คำถามวัดกึ๋น:** "รีเฟรชหน้าเว็บแล้วของหายไหม?"
    *   *คำตอบ:* "ไม่หายครับ เพราะผม Sync State ลง `localStorage` ไว้ด้วย พอกลับมาก็ดึงค่าเดิมมาแสดงต่อได้เลย (ดูที่ `useEffect` บรรทัดที่ 46-48 ครับ)"

---

### 👥 Division of Labor (แบ่งบทพูด)

#### 🟢 หน้าที่ Frontend (ตัวเปิดเกม)
*เน้นเรื่อง UX, ความลื่นไหล, และการจัดการข้อมูลหน้าบ้าน*

**จุดขายที่ต้องพูด:**
1.  **UI/UX:** "เราใช้ดีไซน์แบบ Glassmorphism แยก Theme หน้า User/Admin ชัดเจน เพื่อลดความสับสนในการใช้งานครับ"
2.  **Reusability:** "Component อย่างการ์ดรายการ (`RentalCard.tsx`) ผมเขียนครั้งเดียวแต่ใช้ซ้ำทั้งหน้าประวัติของ User และหน้า Admin ช่วยลดโค้ดซ้ำซ้อนและแก้ Bug ง่ายครับ"
3.  **Smart Validation:** "ระบบเราดัก Errors ตั้งแต่หน้าบ้านครับ เช่น ห้ามเลือกวันย้อนหลัง หรือวันคืนมาก่อนวันยืม ช่วยลด Request ขยะไป Backend"

**สิ่งที่ต้องรู้ของงานเพื่อน (เผื่อโดนจี้):**
*   "Backend เขาใช้ **DTO + class-validator** มาช่วยดัก Type ข้อมูลครับ เช่น `@IsDateString()` หรือ `@IsUUID()` ถ้า Frontend ส่งอะไรแปลกๆ ไป Backend จะตีกลับทันที ไม่พังแน่นอน"

#### 🔵 หน้าที่ Backend (ตัวปิดเกม)
*เน้นเรื่องความถูกต้อง, ความปลอดภัย, และระเบียบวินัยของโค้ด*

**จุดขายที่ต้องพูด:**
1.  **Architecture:** "ผมใช้ NestJS แบ่ง Layer ชัดเจนครับ Controller รับ Request → Service ประมวลผล Business Logic → Repository คุยกับ Database ทำให้โค้ดเป็นระเบียบ แก้ไขง่าย"
2.  **Auto-Reject Logic:** "อันนี้ทีเด็ดครับ พอ Admin อนุมัติเคสหนึ่ง ระบบจะไปไล่ **Auto-Reject** เคสอื่นที่จองซ้อนกันให้อัตโนมัติเลยครับ ดู `handleAutoRejection()` ใน `rentals.service.ts` ตัดปัญหา Human Error"
3.  **Database Design:** "เราแยกตาราง `Equipment` (ข้อมูลรุ่น เช่น Canon 5D Mark IV) กับ `EquipmentItems` (ตัวเครื่องจริงแต่ละชิ้น รหัส 001, 002, ...) เพื่อให้แทร็กของรายชิ้นได้แม่นยำ และความสัมพันธ์นี้เป็นแบบ **One-to-Many** ครับ"

**สิ่งที่ต้องรู้ของงานเพื่อน (เผื่อโดนจี้):**
*   "Frontend เขาใช้ **axios Interceptor** ครับ (ดูที่ `client.ts`) คือตัวกลางที่คอยแนบ Token ไปกับทุก Request ให้อัตโนมัติ และถ้า Token หมดอายุ (401) ก็จะ Redirect ไปหน้า Login เองเลยครับ"

---

### 🛑 คำถามกันตาย (เตรียมคำตอบไว้ อย่าให้ Dead Air)

1.  **"ทำไมเลือกใช้ Tech Stack นี้? (NestJS + React)"**
    *   *ตอบ:* "เพราะมันเป็นภาษา TypeScript ทั้งคู่ครับ (Fullstack TS) ทำให้แชร์ Type หรือ Interface กันง่าย ทีมงานคุยกันรู้เรื่อง และ NestJS มีโครงสร้างที่บังคับให้เขียนโค้ดเป็นระเบียบตามหลัก Separation of Concerns เหมาะกับงานสเกลระดับนี้ครับ"

2.  **"ส่วนไหนแก้ยากที่สุด?"**
    *   *ตอบ:* "เรื่องการจัดการ **Overlap Detection** และการ Auto-Reject ที่ต้องมันทำงานให้ถูกต้องทั้ง Frontend และ Backend ครับ เพราะต้องเทสต์หลายรอบมากเพื่อให้มั่นใจว่าไม่หลุดเคสไหนไปบ้าง"

3.  **"ถ้าให้ทำต่อ อยากเพิ่มอะไร?"**
    *   *ตอบ:* "อยากเพิ่มระบบ **Notification** แจ้งเตือนผ่าน Line หรือ Email ครับ พอ Admin อนุมัติปุ๊บ User รู้เรื่องปั๊บ และอยากเพิ่มระบบ **Penalty** สำหรับคนคืนของเลทครับ"

### 📝 Action Plan คืนนี้
1.  **Frontend:** เปิดไฟล์ `CartContext.tsx` อธิบายเรื่อง **Timer 15 นาที** (`CART_EXPIRY_MINUTES`) และ `setInterval` ให้เพื่อนฟัง
2.  **Backend:** เปิดไฟล์ `rental-validation.service.ts` อธิบาย **Query กันจองซ้อน** (`checkOverlap`) ให้เพื่อนฟัง (บรรทัด 14-34)
3.  **ลองถาม:** "ถ้าเน็ตหลุดตอนกดจอง จะเกิดอะไรขึ้น?" → *คำตอบ: Frontend จะ catch error และแสดง Toast "Network error, please try again" โดยที่ข้อมูลใน Cart ยังอยู่ครบ เพราะเก็บใน localStorage ไว้*

---

ลุยครับ! โปรเจกต์ดีอยู่แล้ว แค่มั่นใจเข้าไว้! 💪
