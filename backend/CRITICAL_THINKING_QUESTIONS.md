# 🎓 คำถาม Critical Thinking สำหรับระบบ Gear Rental Backend

เอกสารนี้รวบรวมคำถามเชิงวิพากษ์สำหรับการประเมินความเข้าใจในกระบวนการทำงานของระบบ Backend

---

## 📋 สารบัญ

1. [Architecture & Design Patterns](#1-architecture--design-patterns)
2. [Authentication & Security](#2-authentication--security)
3. [Database Design & TypeORM](#3-database-design--typeorm)
4. [Business Logic - Rental System](#4-business-logic---rental-system)
5. [Stock & Inventory Management](#5-stock--inventory-management)
6. [Audit Logging System](#6-audit-logging-system)
7. [Error Handling & Validation](#7-error-handling--validation)
8. [Performance & Scalability](#8-performance--scalability)
9. [Code Quality & Best Practices](#9-code-quality--best-practices)
10. [Testing & Debugging](#10-testing--debugging)

---

## 1. Architecture & Design Patterns

### คำถามที่ 1
ทำไมต้องแยก Module ออกเป็น `auth`, `users`, `equipments`, `rentals`, `audit-logs` แทนที่จะรวมไว้ใน Module เดียว? อธิบายข้อดีและข้อเสียของ **Modular Architecture** ในบริบทของ NestJS

### คำถามที่ 2
ใน `RentalsModule` มีการแยก Service ออกเป็น 3 ส่วน คือ `RentalsService`, `RentalValidationService`, และ `RentalStockService` การแยกเช่นนี้สอดคล้องกับหลักการ **Single Responsibility Principle** อย่างไร? และถ้าไม่แยก จะเกิดปัญหาอะไรบ้าง?

### คำถามที่ 3
ระบบใช้ **State Machine Pattern** สำหรับจัดการ Rental Status เหตุใดจึงไม่ใช้การเปลี่ยนสถานะแบบ free-form? อธิบายประโยชน์ของการมี `allowedTransitions` map

### คำถามที่ 4
อธิบายความแตกต่างระหว่าง `@Module()`, `@Controller()`, และ `@Injectable()` decorators ใน NestJS และบทบาทของแต่ละอย่างในระบบนี้

### คำถามที่ 5
ทำไม `AppModule` ถึงใช้ `TypeOrmModule.forRootAsync()` แทน `TypeOrmModule.forRoot()` ตรง? ข้อดีของ Async Configuration คืออะไร?

---

## 2. Authentication & Security

### คำถามที่ 6
ใน `AuthService.validateUser()` ทำไมถึงใช้ `bcrypt.compare()` แทนการเปรียบเทียบ password โดยตรง? อธิบายหลักการทำงานของ bcrypt hashing

### คำถามที่ 7
JWT payload ในระบบนี้ประกอบด้วย `{ sub: user.id, studentId: user.studentId, role: user.role }` ทำไมถึงไม่ใส่ข้อมูลอื่นเช่น `name` หรือ `password` ลงไป?

### คำถามที่ 8
เมื่อ `validateUser()` return ค่า จะทำการ destructure `const { password, ...result } = user;` แล้ว return `result` ทำไมต้อง exclude password ออกจาก result?

### คำถามที่ 9
ถ้า JWT token ถูกขโมยไป ผู้ไม่หวังดีสามารถทำอะไรได้บ้าง? และระบบควรมีมาตรการป้องกันอย่างไร?

### คำถามที่ 10
ระบบใช้ `ThrottlerModule.forRoot([{ ttl: 60000, limit: 100 }])` อธิบายว่า Rate Limiting นี้ช่วยป้องกันการโจมตีประเภทใดบ้าง?

### คำถามที่ 11
ทำไมระบบถึงใช้ `studentId` เป็นตัว login แทนที่จะใช้ email? มีข้อดีข้อเสียอย่างไรในบริบทของระบบยืม-คืนอุปกรณ์ในสถานศึกษา?

---

## 3. Database Design & TypeORM

### คำถามที่ 12
ทำไมตาราง `users` ถึงใช้ `@PrimaryGeneratedColumn('uuid')` แทน auto-increment integer? อธิบายข้อดีและข้อเสียของ UUID เป็น Primary Key

### คำถามที่ 13
ในความสัมพันธ์ระหว่าง `Equipment` และ `EquipmentItem` ใช้ `@OneToMany` พร้อม options `{ cascade: true, eager: true }` อธิบายว่า `cascade` และ `eager` ทำหน้าที่อะไร และส่งผลต่อ performance อย่างไร?

### คำถามที่ 14
ใน `Rental` entity มี foreign key ไปยัง `EquipmentItem` พร้อม `{ nullable: true, onDelete: 'SET NULL' }` อธิบายว่าทำไมต้องใช้ `SET NULL` แทน `CASCADE` ในกรณีนี้?

### คำถามที่ 15
ระบบใช้ `synchronize: true` ใน TypeORM config สำหรับ Development ทำไมถึงไม่ควรใช้ใน Production? และควรใช้วิธีใดแทน?

### คำถามที่ 16
`AuditLog` entity เก็บ `username` ซ้ำกับการมี relation ไปยัง `User` ทำไมต้องเก็บซ้ำ? นี่คือ **Denormalization** หรือไม่ และมีประโยชน์อย่างไร?

### คำถามที่ 17
อธิบายความแตกต่างระหว่าง `@Column({ type: 'enum', enum: RentalStatus })` กับการเก็บ status เป็น `varchar` ปกติ ข้อดีของการใช้ PostgreSQL enum คืออะไร?

### คำถามที่ 18
ในการ query หา overlapping rentals ใช้เงื่อนไข `rental.startDate < :endDate AND rental.endDate > :startDate` อธิบายหลักการทางตรรกศาสตร์ว่าทำไมเงื่อนไขนี้ถึงตรวจจับ overlap ได้ครบทุกกรณี?

---

## 4. Business Logic - Rental System

### คำถามที่ 19
ใน `RentalsService.create()` มีการตรวจสอบ `if (start >= end)` ทำไมต้องใช้ `>=` แทน `>` ถ้าใช้ `>` จะมีปัญหาอะไร?

### คำถามที่ 20
ระบบมี flag `allowOverlap` ที่ให้ user เลือกได้ อธิบาย use case ที่จำเป็นต้องยอมให้ overlap และข้อดีข้อเสียของ design นี้

### คำถามที่ 21
ฟังก์ชัน `handleDuplicateRequests()` ยกเลิกคำขอเก่าอัตโนมัติเมื่อ user ส่งคำขอใหม่ที่ซ้ำซ้อน ทำไมต้องทำแบบนี้แทนที่จะ throw error บอก user ให้ยกเลิกเอง?

### คำถามที่ 22
เมื่อ Admin อนุมัติ rental หนึ่งรายการ ระบบจะ auto-reject rental อื่นที่ overlap ทำไมต้องทำ rejection อัตโนมัติ? และถ้าไม่ทำ จะเกิดปัญหาอะไร?

### คำถามที่ 23
ใน `handleAutoRejection()` มีการ check `if (other.id === rental.id || other.status !== RentalStatus.PENDING) continue;` ทำไมต้อง check ทั้งสองเงื่อนไข?

### คำถามที่ 24
ทำไม `updateStatus()` ถึง return `{ ...savedRental, autoRejectedRentals: ... }` แทนที่จะ return แค่ `savedRental`? ประโยชน์ในแง่ UX คืออะไร?

### คำถามที่ 25
อธิบาย flow ทั้งหมดตั้งแต่ user สร้างคำขอยืม จนถึงได้รับอนุมัติ ระบุทุกขั้นตอนที่ backend ทำและการเปลี่ยนแปลงข้อมูลที่เกิดขึ้น

### คำถามที่ 26
ถ้า user A ส่งคำขอยืมวันที่ 10-15 และ user B ส่งคำขอยืมวันที่ 12-18 (สำหรับอุปกรณ์เดียวกัน) แล้ว Admin อนุมัติ user A จะเกิดอะไรขึ้นกับคำขอของ user B?

### คำถามที่ 27
ใน valid status transitions ทำไม `CHECKED_OUT` ถึงเปลี่ยนได้แค่ `RETURNED` ไม่สามารถเปลี่ยนเป็น `CANCELLED` ได้? มีเหตุผลทาง business อะไร?

---

## 5. Stock & Inventory Management

### คำถามที่ 28
ระบบมี **Two-Level Inventory** คือ `Equipment` (ประเภท) และ `EquipmentItem` (ชิ้นงาน) อธิบายข้อดีของการออกแบบแบบนี้เทียบกับการเก็บแค่ `Equipment` ตัวเดียว

### คำถามที่ 29
เมื่อสร้าง `Equipment` ใหม่ที่มี `stockQty = 5` ระบบจะสร้าง `EquipmentItem` กี่ record? และ `itemCode` จะเป็นอย่างไร?

### คำถามที่ 30
ใน `handleCheckout()` มีการ check `if (equipment.stockQty <= 0) throw new BadRequestException('Equipment is out of stock!')` แต่ในขั้นตอนก่อนหน้านั้นได้ตรวจ overlap แล้ว ทำไมยังต้อง check อีกครั้ง?

### คำถามที่ 31
เมื่อ `stockQty` ลดเหลือ 0 ระบบจะเปลี่ยน `Equipment.status` เป็น `UNAVAILABLE` แล้วเมื่อไหร่ที่จะกลับเป็น `AVAILABLE`? อธิบายเงื่อนไขใน `handleReturn()`

### คำถามที่ 32
ถ้า Admin เพิ่ม `stockQty` จาก 3 เป็น 5 ระบบจะจัดการ `EquipmentItem` อย่างไร? และ `itemCode` ใหม่จะเป็นเลขอะไร?

### คำถามที่ 33
ถ้ามีปัญหา race condition เกิดขึ้นใน `handleCheckout()` เช่น 2 requests มาพร้อมกัน จะเกิดปัญหา stock ติดลบได้หรือไม่? ระบบควรแก้ไขอย่างไร?

---

## 6. Audit Logging System

### คำถามที่ 34
ทำไมระบบถึงมี Audit Logging? อธิบายประโยชน์ในแง่ **Accountability** และ **Debugging**

### คำถามที่ 35
`AuditLog` บันทึก `details` เป็น JSON string ทำไมไม่ใช้ JSONB column type ของ PostgreSQL โดยตรง? มีข้อดีข้อเสียอย่างไร?

### คำถามที่ 36
มี action type `RENTAL_AUTO_CANCELLED` และ `RENTAL_AUTO_REJECTED` แยกออกมาทำไม? ต่างจาก `RENTAL_CANCELLED` และ `RENTAL_REJECTED` ปกติอย่างไร?

### คำถามที่ 37
ฟังก์ชัน `deleteOlderThan(days)` มีไว้ทำอะไร? และทำไมต้องมี Log Retention Policy?

### คำถามที่ 38
ถ้าต้องการหา audit trail ว่า rental ID หนึ่งถูกเปลี่ยนสถานะกี่ครั้ง และใครเป็นคนทำ จะ query อย่างไร?

---

## 7. Error Handling & Validation

### คำถามที่ 39
`ValidationPipe` ใช้ options `{ whitelist: true, forbidNonWhitelisted: true }` อธิบายว่าแต่ละ option ทำอะไร และป้องกันการโจมตีประเภทใด

### คำถามที่ 40
ทำไม `transform: true` ใน ValidationPipe ถึงสำคัญ? ถ้าไม่มีจะเกิดปัญหาอะไรกับ DTO properties ที่เป็น Date หรือ number?

### คำถามที่ 41
ใน `RentalsService.findOne()` ถ้าหา rental ไม่เจอ จะ throw `NotFoundException` ทำไมต้อง throw exception แทนที่จะ return null?

### คำถามที่ 42
อธิบายความแตกต่างระหว่าง `BadRequestException`, `NotFoundException`, และ `UnauthorizedException` และให้ตัวอย่างสถานการณ์ที่ควรใช้แต่ละอัน

### คำถามที่ 43
`HttpExceptionFilter` ทำหน้าที่อะไร? และทำไมต้องจัดรูปแบบ error response ให้เหมือนกันทุก endpoint?

---

## 8. Performance & Scalability

### คำถามที่ 44
ใน `findAll()` query ดึง rentals ทั้งหมดพร้อม `relations: ['user', 'equipment', 'equipmentItem']` ถ้ามี rentals 10,000 records จะเกิดปัญหาอะไร? และควรแก้ไขอย่างไร?

### คำถามที่ 45
Query สำหรับ check overlap ใช้ `createQueryBuilder()` กับหลายเงื่อนไข ควรมี index บน column ใดบ้างเพื่อให้ query เร็วขึ้น?

### คำถามที่ 46
`@OneToMany` ที่มี `eager: true` จะ load items ทุกครั้งที่ query Equipment เมื่อไหร่ควรใช้ `eager: true` และเมื่อไหร่ไม่ควร?

### คำถามที่ 47
ถ้าต้องรองรับ concurrent users 1,000 คน ระบบปัจจุบันมีจุดอ่อนตรงไหนบ้าง?

### คำถามที่ 48
ทำไม `findActiveByEquipment()` ถึงใช้ `.orderBy('rental.startDate', 'ASC')` การ sort ที่ database level ดีกว่า sort ที่ application level อย่างไร?

---

## 9. Code Quality & Best Practices

### คำถามที่ 49
ทำไม Service ไม่ควร access `Request` object โดยตรง แต่ควรรับ parameters ผ่าน function arguments?

### คำถามที่ 50
ใน `RentalsService` constructor inject `AuditLogsService` เพื่อบันทึก log ทำไมต้อง inject แทนที่จะ import และเรียกใช้ตรง?

### คำถามที่ 51
อธิบายประโยชน์ของการใช้ **Dependency Injection** ใน NestJS และสิ่งที่เกิดขึ้นเมื่อประกาศ `@Injectable()` บน class

### คำถามที่ 52
ทำไมควรใช้ DTO แยกสำหรับ Create และ Update operations? (`CreateRentalDto` vs `UpdateRentalStatusDto`)

### คำถามที่ 53
ในโค้ด `const { password, ...result } = user;` ใช้ destructuring เพื่ออะไร? มีวิธีอื่นที่ทำได้หรือไม่?

### คำถามที่ 54
ทำไม `enum` values ถึงถูก define ทั้งใน TypeScript (`RentalStatus`) และใน database schema? และถ้าไม่ sync กันจะเกิดอะไรขึ้น?

---

## 10. Testing & Debugging

### คำถามที่ 55
ถ้าต้องเขียน Unit Test สำหรับ `handleAutoRejection()` ควร mock อะไรบ้าง และควร test cases อะไรบ้าง?

### คำถามที่ 56
`LoggerMiddleware` บันทึก `${req.method} ${req.url}` ทุก request ถ้าต้องการ debug ปัญหา rental creation ควรเพิ่ม log อะไรอีกบ้าง?

### คำถามที่ 57
ถ้า user report ว่า "ส่งคำขอยืมแล้วหายไป" จะ debug อย่างไร? ข้อมูลอะไรใน Audit Log ที่จะช่วยได้?

### คำถามที่ 58
ถ้าต้อง Integration Test สำหรับ flow "สร้างคำขอ → อนุมัติ → เช็คเอาท์ → คืน" ควรจัดเตรียม test data อะไรบ้าง?

---

## 🎯 คำถามสังเคราะห์ขั้นสูง

### คำถามที่ 59
ถ้าต้องเพิ่มฟีเจอร์ "จองล่วงหน้าได้ไม่เกิน 30 วัน" ต้องแก้ไขส่วนไหนของ code บ้าง และต้องเพิ่ม validation ที่ไหน?

### คำถามที่ 60
ถ้าต้องรองรับ "หลายสาขา" (branches) ที่แต่ละสาขามี equipments แยกกัน ต้อง redesign database schema อย่างไร?

### คำถามที่ 61
ถ้าต้องเพิ่มระบบ notification แจ้ง user เมื่อ rental ถูก approved/rejected ควร implement ที่ layer ไหน และใช้ pattern อะไร?

### คำถามที่ 62
อธิบายว่าถ้าต้อง scale ระบบนี้ให้รองรับหลายมหาวิทยาลัย (multi-tenant) ต้องเปลี่ยนแปลง architecture อย่างไร?

### คำถามที่ 63
ถ้าต้องเพิ่มฟีเจอร์ "ยืมต่อ" (extend rental) โดย user สามารถขอ extend endDate ได้หนึ่งครั้ง ต้องเพิ่ม fields และ business logic อะไรบ้าง?

### คำถามที่ 64
ในมุมมอง **Data Integrity** ระบบปัจจุบันมีจุดอ่อนตรงไหนบ้าง? เช่น ถ้า database transaction fail กลางทาง

### คำถามที่ 65
ถ้าต้อง implement **Soft Delete** สำหรับ Equipment (แทนการลบจริง) ต้องแก้ไขอะไรบ้าง และมีผลกระทบกับ queries และ relations อย่างไร?

---

## 📝 แนวทางการตอบ

สำหรับทุกคำถาม ควรตอบโดย:
1. **อธิบายหลักการ/แนวคิด** ที่เกี่ยวข้อง
2. **ยกตัวอย่างจากโค้ดจริง** ในระบบ
3. **อภิปรายข้อดี-ข้อเสีย** ของ approach ที่ใช้
4. **เสนอทางเลือกอื่น** (ถ้ามี) และเปรียบเทียบ

---

*เอกสารนี้สร้างเมื่อ: January 2026*
